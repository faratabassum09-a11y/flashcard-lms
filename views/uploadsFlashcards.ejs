<% layout("layouts/boilerplate") %>

<div style="padding:40px">

  <h1>Flashcard Excel Uploads</h1>

  <br>

  <% if(files.length === 0){ %>

    <h3>No flashcard uploads found </h3>

  <% } %>

  <% files.forEach(file => { %>

    <div style="
      padding:15px;
      margin-bottom:10px;
      border:1px solid #444;
      border-radius:10px;
    ">

      <h3><%= file %></h3>

      <a href="/uploads/flashcards/<%= file %>" target="_blank">
        View File
      </a>

      <br><br>

      <form
        action="/delete-flashcard-upload/<%= file %>?_method=DELETE"
        method="POST"
      >
        <button type="submit">
          Delete
        </button>
      </form>

    </div>

  <% }) %>

</div>